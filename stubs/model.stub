<?php

declare(strict_types=1);

namespace {{ namespace }};

use Illuminate\Database\Eloquent\Factories\HasFactory;
use Illuminate\Database\Eloquent\Model;
{{ schema_traits }}
{{ imports }}

final class {{ class }} extends Model
{
    use {{ model_traits }};

    protected $fillable = [{{ schema_fillable_array }}];

    protected $casts = [
{{ schema_casts_array }}
    ];

{{ schema_relationships }}{{ relationships }}
}
